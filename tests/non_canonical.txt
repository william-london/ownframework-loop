# OwnFramework Loop — non-canonical test exclusion list.
#
# Each line is a path relative to repo root. Tests listed here exist in the
# tests/ tree but are intentionally excluded from DIRECT canonical invocation.
# Add a path here ONLY when:
#   * the test is a manual-only fixture (e.g. canary lifecycle scripts),
#   * the test is a debug-only helper not part of the canonical contract, or
#   * direct invocation is deliberately replaced by a canonical wrapper for a
#     documented isolation reason.
#
# The final-source-closure body is still executed in full by the canonical
# test_final_source_closure_hostsafe.sh wrapper. Direct invocation is excluded
# because its synthetic no-manifest fixture must not inherit a commissioned
# operator workstation's real XDG host-capability authority.
tests/integration/test_final_source_closure.sh
