name: Final authority closure v2

on:
  push:
    branches:
      - hardening/github-authority-closure-2026-09-22
    paths:
      - .github/workflows/final-authority-closure-v2.yml
      - .github/final_authority_closure_v2.py

permissions:
  contents: write

jobs:
  close:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
        with:
          ref: hardening/github-authority-closure-2026-09-22
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Install bounded uv authority
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v uv >/dev/null 2>&1; then
            python3 -m pip install --break-system-packages uv || python3 -m pip install uv
          fi
          uv_source="$(command -v uv)"
          rm -rf /tmp/ofloop-ci-bin
          install -d -m 0755 /tmp/ofloop-ci-bin
          install -m 0755 "$uv_source" /tmp/ofloop-ci-bin/uv
          echo "/tmp/ofloop-ci-bin" >> "$GITHUB_PATH"
          export PATH="/tmp/ofloop-ci-bin:$PATH"
          test "$(command -v uv)" = "/tmp/ofloop-ci-bin/uv"
          uv --version
      - name: Apply and prove authority closure
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${GITHUB_SHA}"
          python3 .github/final_authority_closure_v2.py
          python3 - <<'PY'
          from pathlib import Path
          p = Path('lib/ownframework_loop/integrity.py')
          text = p.read_text(encoding='utf-8')
          if 'from .util import read_json\n' not in text:
              anchor = 'from typing import Any\n'
              if text.count(anchor) != 1:
                  raise SystemExit('integrity read_json import anchor missing')
              text = text.replace(anchor, anchor + '\nfrom .util import read_json\n', 1)
              p.write_text(text, encoding='utf-8')
          PY
          export PYTHONPATH="$PWD/lib${PYTHONPATH:+:$PYTHONPATH}"
          python3 -m compileall -q lib/ownframework_loop bin/ofloop
          probe="$(mktemp -d)"
          git -C "$probe" init -b master >/dev/null
          git -C "$probe" config user.email test@local
          git -C "$probe" config user.name test
          echo seed > "$probe/README.md"
          git -C "$probe" add README.md
          git -C "$probe" commit -m init >/dev/null
          bin/ofloop spec new "$probe" state-creation-probe
          rm -rf "$probe"
          bash tests/unit/test_trust_approval.sh
          bash tests/unit/test_approval_pty_e2e.sh
          bash tests/integration/test_v061_runner_cost_proof.sh
          bash tests/integration/test_v10d_pre1_adversarial_fixes.sh
          bash tests/integration/test_v061_evidence_fail_closed.sh
          bash tests/integration/test_event_chain_concurrency_e2e.sh
          bash tests/unit/test_final_hardening_process_and_lock.sh
          bash tests/integration/test_v121_validation_authority_closure.sh
          ./validate.sh
          git diff --check
      - name: Publish and retire temporary machinery
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin hardening/github-authority-closure-2026-09-22
          test "$(git rev-parse HEAD)" = "$(git rev-parse origin/hardening/github-authority-closure-2026-09-22)"
          git rm .github/workflows/final-authority-closure-v2.yml \
                 .github/final_authority_closure_v2.py \
                 .github/workflows/final-validation-runner-closure.yml
          git add lib/ownframework_loop/process_runner.py \
                  lib/ownframework_loop/cli.py \
                  lib/ownframework_loop/integrity.py \
                  lib/ownframework_loop/state.py \
                  lib/ownframework_loop/execution_start.py \
                  lib/ownframework_loop/validation_executor.py \
                  tests/integration/test_v061_runner_cost_proof.sh \
                  tests/integration/test_v10d_pre1_adversarial_fixes.sh \
                  tests/integration/test_v061_evidence_fail_closed.sh \
                  tests/unit/test_final_hardening_process_and_lock.sh
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'hardening: close lifecycle and event authority gaps'
          git push origin HEAD:hardening/github-authority-closure-2026-09-22
