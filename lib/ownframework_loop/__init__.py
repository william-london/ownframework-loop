"""OwnFramework Loop — durable execution plane for AI coding agents."""
from __future__ import annotations

# Historical release v0.9.1 is FROZEN at the SHA that the v0.9.1 tag
# points at. Current source is the 1.0.0 release candidate; source version
# truth does not imply publication. Publication authority is the immutable
# Git tag together with its GitHub Release. The runtime_generation payload
# derives from the installed bytes, not from this string.
__version__ = "1.0.0"
__all__ = ["__version__"]
